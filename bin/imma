#!/usr/bin/env bash

function main {
  case "${1:-}" in
    login)
      shift
      fogg login "${BASH_SOURCE##*/}" "$@"
      ;;
    *)
      fogg assume "${BASH_SOURCE##*/}" "$@"
      ;;
  esac
}

source sub-chain imma "$@"
