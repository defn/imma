#!/usr/bin/env bash

export CONTEXT_ORG="${0##*/}"
[[ -z "${AWS_DEFAULT_REGION:-}" ]] && source aws-us-east-1

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"
  source normalize

  exec "$@"
}

source sub-chain "$BASH_SOURCE" "$@"
