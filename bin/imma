#!/usr/bin/env bash

function main {
  case "${1:-}" in
    login|assume)
      local nm_cmd="$1"; shift
      fogg "$nm_cmd" "${BASH_SOURCE##*/}" "$@"
      ;;
    *)
      fogg env "${BASH_SOURCE##*/}" "$@"
      ;;
  esac
}

unset AWS_VAULT
source sub-chain imma "$@"
