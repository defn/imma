#!/usr/bin/env bash

export CONTEXT_ENV="${0##*-}"

function main {
  local shome="${_imma_home:-"$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"}"
  source "$shome/script/profile"
  source normalize

  exec exec "$@"
}

source sub-middleware "$BASH_SOURCE" "$@"
